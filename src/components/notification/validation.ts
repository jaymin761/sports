import { NextFunction, Request, Response } from "express"
import validator from "../../utils/validate";
import {  ReportToType } from "../../utils/enum";

const readValidation = async (req: Request, res: Response, next: NextFunction) => {    
    
}

export default {
    readValidation,
}